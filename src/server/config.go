package main

// projectPrefix is the prefix for all environment variables used by this project.
// Change this value to rename all env vars in one place.
const projectPrefix = "BIC"

// targetDir is the directory from which the site is served.
const targetDir = "dist"
