{{define "organization-card"}}
<article class="organization-card">
    <div class="org-header">
        <h3>{{.Name}}</h3>
        <a href="{{.Website}}" target="_blank" class="org-button">Visit Site</a>
    </div>
    <p>{{.Description}}</p>
</article>
{{end}}
