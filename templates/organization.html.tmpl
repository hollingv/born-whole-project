{{define "organization-card"}}
<article class="organization-card">
    <a href="{{.Website}}">
        <img src="{{.Thumbnail}}" alt="{{.Name}}">
    </a>
    <h3>{{.Name}}</h3>
    <p>{{.Description}}</p>
</article>
{{end}}
